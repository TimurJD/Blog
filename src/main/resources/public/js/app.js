define([
    'Backbone'
], function (Backbone) {
    function init() {
    	alert("AAA");
//        var router = new Router();
//
//        APP.router = router || {};
//        Backbone.history.start();
    }

    return {
        init: init
    }
});
